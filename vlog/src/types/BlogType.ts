export interface BlogType {
  title: string;
  date: string;
  desc: string;
  name: string;
}

export interface BlogTypeAdID {
  title: string;
  date: string;
  desc: string;
  name: string;
  id: number;
}
