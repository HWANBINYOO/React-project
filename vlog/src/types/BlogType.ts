export interface BlogType {
  // BlogImg: string;
  date: string;
  title: string;
  desc: string;
  id: number;
  name: string;
}

export interface BlogTypeH {
  BlogImg: string;
  date: string;
  title: string;
  desc: string;
  id: number;
  HeaderColor: string;
}
