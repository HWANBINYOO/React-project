export interface BlogType {
  BlogImg: string;
  date: string;
  title: string;
  desc: string;
  id: string;
}

export interface BlogTypeH {
  BlogImg: string;
  date: string;
  title: string;
  desc: string;
  id: string;
  HeaderColor: string;
}
